num = int(input("enter-"))
digit = int(input("enter-"))
digit1 =int(input("enter-"))
count1 = 0
i = 0
count = 0
while i < num:
    rem = num % 10
    if rem == digit:
        count += 1
    elif rem == digit1:
        count1 += 1
    num = num // 10
print(f"{digit}'s in num = {count}")
print(f"{digit1}'s in num = {count1}")